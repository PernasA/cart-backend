ktor:
    application:
        modules:
            - com.ApplicationKt.module
    deployment:
        port: 8080

db:
    url: "jdbc:postgresql://localhost:5432/cart_db"
    user: "postgres"
    password: "postgres"
